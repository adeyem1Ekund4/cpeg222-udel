#include "hardware_init.h"
#include <xc.h>
#include "lcd.h"
#include "ssd.h"

void Hardware_Init(void) {
    // Set TRIS, ANSEL, etc. as in your original initialize_ports()
    // Call LCD_Init(), SSD_Init(), etc.
}
